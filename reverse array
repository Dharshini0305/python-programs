# include<stdio.h>
int main(){
	int a[10],i,size;
	printf("Enter the number of elements in an array:");
	scanf("%d",&size);
	printf("Enter the elements of the array:");
	for(i=0;i<size;i++)
	{
		scanf("%d",&a[i]);
	}
	for (i=size-1;i>=0;i--)
	{
		printf("%d",a[i]);
	}
	return 0;
}
