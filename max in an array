# include<stdio.h>
int main(){
	int a[10],i,size,n,max;
	printf("Enter the number of elements in an array:");
	scanf("%d",&size);
	printf("Enter the elements of the array:");
	for(i=0;i<size;i++)
	{
		scanf("%d",&a[i]);
	}
	max=a[0];
	for(i=0;i<size;i++){
		if (a[i]>max){
		max=a[i];
		}
	}
	printf("The maximum value in the array is : %d",max);
	return 0;
	}
