# include<stdio.h>
int main(){
	char word[50];
	int length=0;
	printf("Enter the word:");
	scanf("%s",&word);
	for(length=0;word[length] != '\0';length++){
	}
	printf("The length of the given word is %d",length);
	return 0;
}
