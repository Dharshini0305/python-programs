# include<stdio.h>
int main(){
	char word[50];
	int i;
	printf("Enter the word in upper case:");
	scanf("%s",word);
	for(i=0;word[i]!='\0';i++){
		if(word[i]>='A' && word[i]<='Z'){
			word[i]=word[i]+32;
		}
	}
	printf("The lowercase string: %s",word);
	return 0;
}
